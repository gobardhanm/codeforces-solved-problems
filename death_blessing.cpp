#include<bit/stdc++.h>
using namespace std;

#define ll long long

void solve() {
    
    (will upsolve)
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int tc = 1;
    cin >> tc;
    for(int i = 1; i <= tc; i++) {
        solve();
    }
    
    return 0;
}
